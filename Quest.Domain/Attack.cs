using System.Collections.Generic;

namespace Quest.Domain;

public class Attack {
  private readonly Player _attacker;
  private readonly Territory _target;
  private readonly List<ArmyUnit> _attackingUnits;

  // Constructor, getters and setters
}